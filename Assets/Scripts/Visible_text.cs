using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Visible_text : MonoBehaviour {

    /*
    List<String> dialogoCompleto = new List<String>();
    int dialogoActual = ;
    public Text textoVisible;

    // Use this for initialization
    void Start () {
        dialogoCompleto.Add("soy el primer cuadro de texto");
        dialogoCompleto.Add("soy el segundo cuadro de texto");
        dialogoCompleto.Add("soy el tercer cuadro de texto");

        textoVisible.text = dialogoCompleto[dialogoActual];
    }
	
	// Update is called once per frame
	void Update () {
        if (Input.GetKeyDown(KeyCode.P))
        {
            dialogoActual++;

            if (dialogoActual == dialogoCompleto.Count)
            {   //termino de pasar
                //desactivar y resetear el dialgo hasta que se muestra otra vez
            }
            else
            {
                textoVisible.text = dialogoCompleto[dialogoActual];
            }

        }

    }*/
}
